# AnnotationCheck 高危风险清单（卡死/崩溃专项）

> 结论基于：本地实际启动测试 + 真实数据集快速基准 + 关键路径代码审查。  
> 目标聚焦“运行时突然卡死”和“高概率崩溃”。

---

## 风险 1：编辑后全量校验在主线程执行，易造成明显卡顿（高危）

- **位置**：`ui/main_window.py` 的 `_on_line_edited()`、`_after_undo_redo()`、`_on_rewrite_finished()`
- **现象**：每次编辑提交都会调用 `validator.validate_all(self.ann_mgr.lines)`，并在 UI 主线程里完成。
- **实测证据**：
  - 真实序列（262 行）全量校验耗时约 `0.292s/次`
  - 20k 行模拟文本全量校验约 `0.409s/次`
- **卡死机理**：用户连续“编辑 -> 切帧 -> 编辑”时，主线程被频繁占用，界面出现“短冻结”，体感接近卡死。
- **建议优先级**：`P0`
- **修复建议**：
  1. 编辑路径改用 `validate_range()` 增量校验（当前已具备该接口）。
  2. 对高耗时序列增加节流（例如 100~200ms 合并更新）。
  3. 批量改写结束后的全量校验可放后台线程，主线程只做最终替换。

---

## 风险 2：图像解码在主线程同步执行，翻帧时容易卡住（高危）

- **位置**：`ui/image_panel.py` -> `ImageView.set_image()` -> `core/image_loader.py::load_pixmap()`
- **现象**：每次切帧同步加载图片，且 `load_pixmap()` 内部使用 Pillow + PNG 中转 + Qt 转换，CPU/I/O 成本较高。
- **实测证据**：
  - 真实数据 60 张图平均解码约 `63.55ms/张`
  - 一次切帧通常涉及双模态两张图，理论主线程阻塞可达 `100ms+`
- **卡死机理**：快速拖动滑条/连按方向键时，主线程堆积同步解码任务，界面容易“假死”。
- **建议优先级**：`P0`
- **修复建议**：
  1. 图像加载改为后台线程 + 主线程回填。
  2. 建立 LRU 帧缓存（当前帧及邻帧）。
  3. 降低无效重绘和重复解码（同帧重复访问直接命中缓存）。

---

## 风险 3：导航时同步写文件（文本+JSON），高频 I/O 造成界面阻塞（高危）

- **位置**：
  - `ui/main_window.py::_go_to_frame()` 中 `ann_mgr.save_minimal()`
  - `core/review_manager.py::add_flag()/remove_flag()` 每次都 `_save_json()`
- **现象**：切帧时如果有修改，立即同步落盘；标记操作也会同步写 JSON。
- **卡死机理**：磁盘慢、杀毒扫描、网络盘等场景下，同步 I/O 会直接阻塞主线程，表现为随机卡顿甚至无响应。
- **建议优先级**：`P0`
- **修复建议**：
  1. 导航改为“脏标记 + 定时批量落盘”。
  2. 标记文件采用 debounce（如 500ms~1s 合并写）。
  3. 使用原子写（临时文件 + rename）并增加写入失败日志。

---

## 风险 4：翻译请求线程无上限，快速翻帧会触发线程风暴（高危）

- **位置**：`ui/text_panel.py::_schedule_translate()` / `_translate_async()`
- **现象**：当前帧翻译 + 邻帧 `±1..±3` 预取均通过 `threading.Thread` 直接创建；没有线程池上限、没有速率控制。
- **卡死机理**：高频翻帧时短时间创建大量线程，CPU/网络拥塞，GIL 竞争与回调积压使 UI 卡死概率上升。
- **建议优先级**：`P0`
- **修复建议**：
  1. 使用 `QThreadPool`/`ThreadPoolExecutor` 限制并发数（如 2~4）。
  2. 预取降级为 `±1` 或动态策略。
  3. 增加取消/过期机制（离开帧后旧任务可丢弃）。

---

## 风险 5：`FlagDialog` 对新类型映射不完整，可直接触发崩溃（高危）

- **位置**：`ui/flag_dialog.py`
- **现象**：`FLAG_TYPES` 包含 `AI_GENERATED`，但 `labels` 字典没有该 key。循环构建单选框时使用 `labels[ftype]`，会抛 `KeyError`。
- **触发条件**：打开标记弹窗时遍历到 `AI_GENERATED`。
- **影响**：直接异常中断（崩溃级）。
- **建议优先级**：`P0`
- **修复建议**：
  1. 补全 `AI_GENERATED` 显示文案。
  2. 改为 `labels.get(ftype, ftype)` 防御式读取。
  3. 增加启动时自检：`FLAG_TYPES` 与 UI 文案表一致性校验。

---

## 风险 6：批量改写线程与主线程共享可变数据，存在竞态与状态错乱风险（高危）

- **位置**：`ui/main_window.py::_RewriteThread`（`ann_lines` 直接引用主线程列表）
- **现象**：后台线程读取 `self.ann_lines`，同时主线程可能继续编辑或切帧更新文本。
- **卡死/异常机理**：竞态导致结果不一致、逻辑跳变，异常情况下回调频繁重算可放大 UI 卡顿。
- **建议优先级**：`P1`
- **修复建议**：
  1. 启动线程时传入 `ann_lines` 的不可变快照（`list(self.ann_mgr.lines)`）。
  2. 主线程在改写期间限制关键编辑操作或采用版本戳校验。
  3. 回填结果前检查序列是否仍为当前序列。

---

## 风险 7：异常被大量吞掉，卡死根因难以定位（高危运维风险）

- **位置**：`core/config_manager.py`、`core/annotation_manager.py`、`core/review_manager.py`、`core/image_loader.py` 等
- **现象**：多处 `except Exception: pass`，错误不记录、不告警。
- **影响**：真实故障（I/O失败、JSON损坏、权限问题）会变成“偶发卡死/异常行为但无日志”，严重拖慢排障。
- **建议优先级**：`P1`
- **修复建议**：
  1. 引入统一日志模块（至少写到本地 rotating log）。
  2. 对关键持久化路径（保存、加载、翻译）保留错误上下文。
  3. UI 层对可恢复错误给出非阻塞提示（状态栏/轻提示）。

---

## 建议的修复顺序（按收益）

1. 先做 **P0**：增量校验、异步图像加载、I/O 合并写、翻译并发上限、`FlagDialog` 崩溃修复。  
2. 再做 **P1**：批量改写线程数据隔离、统一日志。  
3. 修复后补充回归测试：大序列快速翻帧、连续编辑、批量改写并发、弱磁盘环境测试。

